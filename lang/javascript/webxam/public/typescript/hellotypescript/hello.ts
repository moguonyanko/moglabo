/**
 * @fileoverview 動作確認用TypeScript
 */

const message: string = 'コンパイルされたTypeScript';

const container: any = document.getElementById('hello-container');
container.appendChild(document.createTextNode(message));
